import React from 'react';
import MainContainer from "@/components/MainContainer";

const Contacts = () => {
    return (
        <MainContainer keywords={"contacts"} title={"Контакти"}>
            <h1>Контакти</h1>
        </MainContainer>
    );
};

export default Contacts;